import './bootstrap';
import { initDragDropImage } from './dragDropImage';

document.addEventListener('DOMContentLoaded', () => {
	// Initialize drag & drop image uploader on pages that include the markup
	initDragDropImage();
});
